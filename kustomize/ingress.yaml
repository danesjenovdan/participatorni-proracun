apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pp-volitve-2022-enable-cors
spec:
  headers:
    accessControlAllowOriginList:
      - "https://participativni-proracun.djnd.si"
      - "https://participativni-proracun.danesjenovdan.si"
    accessControlAllowMethods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
    accessControlAllowHeaders:
      - "*"
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pp-volitve-2022-add-trailing-slash
spec:
  redirectRegex:
    regex: (/2022/zaobljube)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pp-volitve-2022-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /2022/zaobljube
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pp-volitve-2022
  annotations:
    # do not put cert manager here, root ingress will handle it
    traefik.ingress.kubernetes.io/router.middlewares: |
      pp-kampanja-pp-volitve-2022-enable-cors@kubernetescrd,
      pp-kampanja-pp-volitve-2022-add-trailing-slash@kubernetescrd,
      pp-kampanja-pp-volitve-2022-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: participativni-proracun.djnd.si
      http: &http_paths
        paths:
          - path: /2022/zaobljube
            pathType: Prefix
            backend:
              service:
                name: pp-volitve-2022
                port:
                  number: 3000
    - host: participativni-proracun.danesjenovdan.si
      http: *http_paths
